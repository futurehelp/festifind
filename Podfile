# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'festifind' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for festifind
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON'
  pod 'SDWebImage', '~> 4.0'
  
  target 'festifindTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'festifindUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
